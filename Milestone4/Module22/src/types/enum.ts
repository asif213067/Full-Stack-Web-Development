enum Direction {
    Up,
    Down,
    Left,
    Right
}

let move: Direction = Direction.Down;
console.log(move);

enum UserRole {
    Admin,
    Guest,
    Moderator
}

enum ResponseStatus {
  Success = "SUCCESS",
  Code = 200
}